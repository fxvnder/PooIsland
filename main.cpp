#include "program.h"
#include "interface.h"

#include <QCoreApplication>
#include <QDebug>
#include <iostream>
#include <vector>

int main(int argc, char *argv[]) {
    //QCoreApplication a(argc, argv);
    //qDebug() << "Hello World";
    //return QCoreApplication::exec();
    run();
    bemvindo();
    tiles pissa;
    std::vector<tiles> p;
}

/*
 * perguntar ao prof se deve meter os construtores no .h?
 *
  Founder explica
    for (int x : vec) // nova forma de usar ciclos for
    cout <<  x << " ";

 Como se usa e para quê que se usa o new

  */